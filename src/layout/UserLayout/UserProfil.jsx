import React from 'react'
import { Link, Outlet } from 'react-router-dom'

export const UserProfil = () => {
    // if (!user) {

    // }
    return (
        <div>
            <Outlet />
        </div>
    )
}
